package com.sparta.product.domain.repository;

import com.sparta.product.domain.core.TimeSaleSoldOut;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TimeSaleSoldOutRepository extends JpaRepository<TimeSaleSoldOut, Long> {
}
